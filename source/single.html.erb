<% content_for :title, "Beyond Code Season #{season.number}: #{season.title} – #{episode.title}" %>
<% content_for :description, "An interview with #{episode.title} at #{season.title}. A Changelog Films production."%>
<% content_for :pull_image, "http://beyondcode.tv/#{episode.image_path}" %>

<%= partial "episode", locals: {episode: episode, parent: "single"} %>

<section class="ctas">
  <%= partial "cta", locals: {season: season} %>
  <%= partial "cta_all" %>
</section>
